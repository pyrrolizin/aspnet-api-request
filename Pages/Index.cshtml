@page
@model IndexModel
@{
    ViewData["Title"] = "ASP.NET API Request";
}

<h1>@ViewData["Title"]</h1>
<p>This page is a demo project and template for a development of a ASP.NET project,
which accesses an external API and caches the result in a cache.</p>
<p>This comes with a responsive template using Bootstrap 5.</p>
<h2 class="pt-3">Weather in Stuttgart</h2>
<div class="weather_info text-center h2"> @Model.Condition &hellip; @Model.Temperature °C</div>
